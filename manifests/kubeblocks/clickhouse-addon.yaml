apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kb-addon-clickhouse
  namespace: kb-system
spec:
  releaseName: kb-addon-clickhouse
  dependsOn:
    - name: kubeblocks
  chart:
    spec:
      chart: clickhouse
      sourceRef:
        kind: HelmRepository
        name: kubeblocks
        namespace: kb-system
      version: "1.0.3"
  interval: 1h0m0s
  install:
    remediation:
      retries: 3
